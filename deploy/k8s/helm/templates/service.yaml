apiVersion: v1
kind: Service
metadata:
  name: {{ include "probotsharp.fullname" . }}
  labels:
    {{- include "probotsharp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "probotsharp.selectorLabels" . | nindent 4 }}
