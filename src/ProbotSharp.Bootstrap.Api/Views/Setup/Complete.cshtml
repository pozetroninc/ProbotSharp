@{
    ViewData["Title"] = "Setup Complete";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var appUrl = ViewData["AppUrl"] as string ?? "";
}

<div class="alert alert-success">
    <strong>Success!</strong> Your GitHub App has been configured.
</div>

<h1>Setup Complete</h1>
<p>Congratulations! Your Probot Sharp app is now configured and ready to receive webhooks from GitHub.</p>

<h2>What's Next?</h2>
<ol class="steps">
    <li>
        <strong>Install your app:</strong> Visit your app's page on GitHub to install it on repositories.
        @if (!string.IsNullOrEmpty(appUrl))
        {
            <br /><a href="@appUrl" target="_blank" style="color: #0969da;">@appUrl</a>
        }
    </li>
    <li><strong>Start your server:</strong> Your app is now ready to receive webhooks. Make sure your server is running.</li>
    <li><strong>Build your app:</strong> Add event handlers to respond to GitHub events like issues, pull requests, and more.</li>
    <li><strong>Test locally:</strong> Use Smee.io or ngrok to receive webhooks during local development.</li>
</ol>

<h2>Useful Commands</h2>
<div class="code-block">
    <pre># Start your Probot Sharp app
dotnet run

# View your app settings
dotnet probot-sharp config

# Test webhook delivery
# Visit your GitHub App settings and send a test webhook</pre>
</div>

<div style="margin-top: 24px;">
    <a href="/" class="btn btn-primary">Go to Dashboard</a>
    @if (!string.IsNullOrEmpty(appUrl))
    {
        <a href="@appUrl" class="btn btn-secondary" style="margin-left: 8px;" target="_blank">View App on GitHub</a>
    }
</div>
